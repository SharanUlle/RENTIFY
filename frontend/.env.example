# Backend API URL (only needed in production when frontend and backend are deployed separately)
# In dev, the Vite proxy handles API calls - leave this unset
VITE_API_URL=https://your-backend-url.onrender.com
